#!/bin/bash --login

# Number of nodes has to match the run script.
#PBS -l select=1
#PBS -N example
#PBS -A <your account, e.g. z01>
#PBS -l walltime=00:01:00

export icon_dir=<where ICON-AES is, especially modules.bash>

. $icon_dir/modules.bash
export LD_LIBRARY_PATH=$CRAY_LD_LIBRARY_PATH:$LD_LIBRARY_PATH

# Run the parallel program which matches the number of nodes in the
# PBS -l select statement.
cd $icon_dir/run
./<your xxxxxx.run script, possibly generated by the ICON-AES tools>
